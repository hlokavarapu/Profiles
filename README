Here are the profile config files necessary to install ASPECT through hashDist.
  * Note, the dependencies for hashDist are python and git.

  To acquire the code run:
  	git clone https://github.com/Vandemar/Profiles.git
	cd Profiles

  To install hashDist run:
    	sh install.sh 

To install Aspect execute the command in a new terminal:
  hit build -j {{Number_of_Processors}} aspect.yaml

If you are trying to install over ssh then execute the command in a new terminal:
  nohup hit build -j {{Number_of_Processors}} aspect.yaml &

