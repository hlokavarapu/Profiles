extends:
- file: hashstack2/linux.yaml

parameters:
  debug: false
  version: 1.1.1 #Calypso version number

packages:
  cmake:
  mpi:
    use: openmpi
  hdf5:  # need to double-check if mpi enabled
  calypso:
  

package_dirs:
- hashstack2/pkgs
- hashstack2/base
- pkgs

  
